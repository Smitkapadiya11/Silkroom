"use client";
import { FormEvent, useState } from "react";
export function SettingsForm({ settings }: { settings: Record<string, string | number | boolean | Date | null> }) {
  const [saved, setSaved] = useState(false);
  async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const data: Record<string, unknown> = Object.fromEntries(new FormData(event.currentTarget)); for (const key of ["combo3PriceInr","combo5PriceInr","unitPriceInr","codFeeInr","prepaidDiscountInr","freeDeliveryThresholdInr","packageWeightGrams","packageLengthCm","packageWidthCm","packageHeightCm"]) data[key] = Number(data[key]); data.announcementEnabled = data.announcementEnabled === "on"; await fetch("/api/admin/settings", { method: "PUT", headers: { "content-type": "application/json" }, body: JSON.stringify(data) }); setSaved(true); }
  const input = (key: string, label: string, type = "text") => <label key={key}>{label}<input type={type} name={key} defaultValue={String(settings[key] ?? "")} /></label>;
  return <form className="admin-form" onSubmit={submit}>{input("contactEmail","Contact email","email")}{input("contactPhone","Contact phone")}{input("returnAddress","Return address")}{input("responseHours","Response hours")}{input("combo3PriceInr","3-piece price","number")}{input("combo5PriceInr","5-piece price","number")}{input("unitPriceInr","Unit price","number")}{input("codFeeInr","COD fee","number")}<label><input type="checkbox" name="announcementEnabled" defaultChecked={Boolean(settings.announcementEnabled)} /> Show announcement</label><label style={{ flexBasis: "100%" }}>Announcement<textarea name="announcementText" defaultValue={String(settings.announcementText ?? "")} /></label><button className="admin-button">Save settings</button>{saved ? <span>Saved.</span> : null}</form>;
}
